apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azurefile-pvc
  namespace: poc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azurefile-static
  volumeName: azurefile-pv
  resources:
    requests:
      storage: 5Gi